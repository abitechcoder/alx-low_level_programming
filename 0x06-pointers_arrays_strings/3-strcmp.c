#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: first parameter
 * @s2: second parameter
 *
 * Return: less than, equal to or greater than 0
 */

int _strcmp(char *s1, char *s2)
{
	int res;

	res = strcmp(s1, s2);
	return (res);
}
